// Test member access (.) operator

// --- List properties ---
nums = list [1, 2, 3, 4, 5]
println(nums.length)
println(nums.size)
println(nums.empty)

empty_list = list []
println(empty_list.empty)
println(empty_list.length)

// --- List no-arg methods ---
data = list [3, 1, 2]
data = reverse(data)
println(len(data))

// --- String properties ---
greeting = 'hello world'
println(greeting.length)

// --- String no-arg methods ---
println(upper('hello'))
println(lower('WORLD'))
println(trim('  hi  '))

// --- Chaining ---
chain_list = list [10, 20, 30]
println(len(reverse(chain_list)))
println(upper('hello').length)

// Note: null.length and int.upper are now caught at compile time by the analyzer
